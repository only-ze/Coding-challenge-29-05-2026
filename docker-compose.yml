services:
  postgres:
    image: postgres:16
    container_name: mundoinvest-db
    restart: always
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${DATABASE_URL} 
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: